# 安全政策

[English](SECURITY.md) · [简体中文](SECURITY.zh-CN.md) · [返回项目首页](README.md)

## 支持版本

在首个稳定版本发布前，只支持 `main` 分支的最新提交。安全修复可能调整私有数据结构或安装器；更新前请阅读发布说明，并保留一份本地备份。

## 私下报告漏洞

不要在公开 Issue 中提交漏洞细节、token、OAuth 文件、用户路径、带签名的媒体 URL、日志、截图或健康数据。

请使用本仓库的 GitHub 私密漏洞报告：

<https://github.com/w2478328197-arch/open-health-agent/security/advisories/new>

请提供使用合成数据的最小复现、受影响的提交或版本、平台、影响，以及已知时的缓解建议。删除所有个人数据和凭据。如果私密报告入口不可用，只创建一条不含技术细节的公开 Issue，请维护者启用私密渠道。

## 安全边界

本项目会防范意外重复写入、工作簿部分保存、常见凭据误提交、无边界命令输出和不必要的数据分享，但无法防护：

- 已被攻破的系统账号或未锁定的电脑；
- 恶意或失陷的模型、消息、可穿戴设备、云同步或 OAuth 提供商；
- 用户使用 `git add -f` 强行提交被忽略的私有文件；
- 未经审阅且含提示注入的第三方 Skill 或 `AGENTS.md`；
- 其他程序覆盖或锁定已打开的工作簿；
- 不准确的健康传感器或模型估算。

把导入的备注、食品标签、工作簿、图片、转写和 API 文本都视为不可信数据，不能把它们当作可执行指令。

## 部署检查

- 私有数据目录只允许所属用户访问。
- 只保留一个共享账本写入器，并停用旧导入任务。
- SQLite 留在本机；只有用户接受云端边界时才同步 Excel 视图。
- 发送任何健康消息前，把 Weixin 私信策略从 `open` 改为 pairing 或本人 allowlist，关闭群聊，并用非敏感内容验证。bot 仍为 open 时不要发送健康数据。
- 限制并检查 Hermes 等宿主的媒体缓存，例如 `~/.hermes/cache/{images,audio,videos,documents}`；不要假设音频、视频或其他媒体会自动删除。
- Google 只授权所需的只读 scope，并遵守 Google 当前验证和数据政策。
- 不要把用户文本插入 shell 命令字符串。
- 日志和错误中删除 token、原始负载和敏感 URL。
- 给备份设置保留边界，并真实测试恢复。
- 发布前运行仓库测试、秘密扫描，并检查暂存差异。
- 停用时撤销凭据并移除后台任务。

## 事件响应

如果凭据或健康记录进入了 Git：

1. 立即撤销或轮换凭据，或断开受影响的提供商；
2. 如果 scheduler 或 gateway 可能继续暴露数据，先停止它们；
3. 使用合适的历史重写方式从工作区和 Git 历史中删除数据；
4. 在可能时使已发布 release 或缓存失效；
5. 通知受影响用户，但不要在通知中再次包含其数据；
6. 检查日志和提供商访问记录；
7. 用不含真实敏感值的 fixture 增加回归测试。

token 一旦暴露，只删除提交并不够，必须轮换。
