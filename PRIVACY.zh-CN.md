# 隐私说明

[English](PRIVACY.md) · [简体中文](PRIVACY.zh-CN.md) · [返回项目首页](README.md)

Open Health Agent 采用本地优先设计：SQLite 真源、私有目标、配置、上下文、备份和 Excel 视图都建立在用户控制的本地数据目录中。本仓库不运营托管式健康数据服务。

“本地优先”不代表所有上游处理都在本机。根据用户启用的功能，下列各方可能处理数据：

- 可穿戴设备厂商及其 App 或云服务；
- Apple Health 或 Health Connect 健康数据存储，以及相应 Apple/Google 系统账号和健康 App 层；
- Google Health 和 Google Health API；
- 腾讯 Weixin iLink Bot API 对微信消息和媒体的处理；
- 所选语言模型提供商，以及独立的语音转写或视觉提供商；
- 放置 Excel 工作簿的 iCloud 或其他同步服务。

连接前应分别阅读这些提供商的条款、隐私说明和保留控制。

## 本机保存的数据

应用可能保存测量值、训练、睡眠/活动汇总、已摄入饮食、营养估算、用户目标原话、用户提供的健康约束、同步元数据、来源/置信度备注、有限审计事件和备份、配置以及不含原始负载的诊断摘要。正常运行不会持久保存完整 `ghealth` 响应。

凭据和真实记录不属于公开仓库。`.gitignore` 会排除常见数据库、工作簿、媒体、环境文件、日志和 token，但这只是防护措施，不能替代发布前检查暂存文件。

## 目的与最小化

数据仅用于维护用户自己的私有账本、导出可读工作簿，并构建用于 wellness/fitness 建议的精简上下文。本项目应当：

- 只请求用户选定的 Google Health 只读 scope；
- 给模型发送最少必要上下文，而不是整个工作簿；
- 不收集任务不需要的精确身份或位置数据；
- 缺失数据保持为空，不制造数值；
- 不长期保留原始提供商负载；按用户选择检查和删除宿主/提供商媒体缓存；
- 从日志中删除凭据和敏感 URL。

Google Health 集成还必须遵守当前的 [Google Health API 开发者和用户数据政策](https://developers.google.com/health/policies/health-api-developer-user-data-policy)、[OAuth 设置要求](https://developers.google.com/health/setup)和 [scope 文档](https://developers.google.com/health/scopes)。

## 用户控制

用户控制本地数据目录，并可以：

- 检查或导出 SQLite/Excel 数据；
- 更正记录，同时保留审计事件；
- 停用小时任务；
- 断开 Google、Weixin 或模型提供商；
- 按已安装宿主版本检查并清理本地消息媒体缓存；
- 把 Excel 视图移出同步目录；
- 撤销提供商凭据；
- 按本地和提供商各自的控制删除数据。

删除仓库不会删除外部提供商持有的数据。删除本地数据目录也不会撤销另一个应用保存的 OAuth token；两种动作可能都需要执行。

部分 Hermes 版本会把消息媒体下载到 `~/.hermes/cache/images`、`audio`、`videos` 和 `documents`。保留时间取决于版本和媒体类型，本项目不承诺自动删除，音频或视频尤其可能继续存在。尽量把缓存限制为仅当前用户可访问，检查已安装版本的行为，并删除不再需要的媒体。提供商一侧的副本必须使用对应提供商的删除控制。

## 图片、语音和辅助模型

微信成功传递图片或语音，不代表所选主模型可以理解它们。图片可能由主视觉模型处理；当主模型是 DeepSeek 等纯文本端点时，Hermes 也可能把图片交给单独的辅助视觉模型，再把文字描述交给主模型。语音转写同样可能由独立 STT 服务处理。启用这些旁路前，应明确知道具体提供商，因为同一条消息可能经过不止一个模型服务。

照片营养、仪表读数和语音转写都可能出错。需要保留来源、置信度和不确定性；诊断影像、医疗判断和高风险决策不能依赖这些估算。

## 分享与发布

本软件不授权把健康数据分享给雇主、保险公司、广告商、其他用户或公开仓库。让 Agent 记录或分析数据，不等于同意发布。Issue、测试、截图和 pull request 只能使用合成数据。

实现层防护见 [Skill 隐私与安全参考](skills/open-health-agent/references/privacy-safety.md)。发现漏洞或意外暴露时，请遵循[安全政策](SECURITY.zh-CN.md)。

## 非医疗声明

Open Health Agent 不是医疗器械、医疗服务提供方、诊断、治疗、处方或紧急监测服务。可穿戴数据和图片估算可能不准确。不能因为本软件而延误专业医疗或急救。
